[package]
name = "vox"
version = "0.3.4"
edition = "2021"
rust-version = "1.87"
description = "A systems level compiler for Vox (sentence based code)"

[dependencies]
thiserror = "2.0"

[profile.release]
opt-level = 3
lto = true
