(Bug #73: as with a number, a text parameter takes one word and was already
 right. Pinned for the same reason.)

For each attempt from 1 to 2,
    If attempt is 99 then, Break.
To greet with a text called name.
    Print name.

greet of "hi".
Exit 0.
