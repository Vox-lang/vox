Remove the directory called "/tmp/vox_test_mkdir_error".

Create a directory called "/tmp/vox_test_mkdir_error".
Create a directory called "/tmp/vox_test_mkdir_error".
On error print "mkdir error detected as expected".

Remove the directory called "/tmp/vox_test_mkdir_error".
