(Bug #66's control: the same program with every declaration moved above the
 function. This is the neighbour that always worked, kept so the fix for the
 forward case cannot be mistaken for a change to the ordinary one.)

a number called counter is 42.
a text called label is "hello".
a float called ratio is 2.5.
a list called items is [1, 2, 3].
a buffer called payload is "ABC".
a map called scores is {"a": 1}.
a boolean called flagged is yes.

To 'show all'.
    Print counter.
    Print label.
    Print ratio.
    Print items.
    Print payload.
    Print scores.
    Print flagged.

'show all'.

Print counter.
Print label.
Print ratio.
Print items.
Print payload.
Print scores.
Print flagged.
