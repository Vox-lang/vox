(Bug #73: a scalar parameter takes one argument word and one word is what
 the empty signature pushed, so this shape was already right. It is a test
 because the fix hands every swallowed definition a real signature, and a
 real signature must not change what was already correct.)

For each attempt from 1 to 2,
    If attempt is 99 then, Break.
To examine with a number called given.
    Print given.

examine of 7.
Exit 0.
