(Stage 1e2 - nested map+list recursive print. A map value may be a list
 or another map; _map_print recurses via the shared _print_depth budget.
 Acceptance: the nested rendering below.)

a map called nested is {"list": [1, 2], "inner": {"x": 1}}.
print nested.
print nested's "list".