(docs/BUGS_FOUND.md #112. Already correct before this fix (bug #43);
 pinned alongside the rest of the type family.)

To 'maybe' with a boolean called 'the choice'.
    If 'the choice' then,
        Return a float, 2.5.

a float called r is 'maybe' of false.
print r.
