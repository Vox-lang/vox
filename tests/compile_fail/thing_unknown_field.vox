(Compile-fail: plan 310 §3 - a thing's fields are its whole member space, so
 a name that is not one of them cannot be a field. The message names the
 thing and lists what it does have.)

A thing called point has
  a number called x is 0,
  a number called y is 0.

a point called origin.
Set origin's z to 1.
