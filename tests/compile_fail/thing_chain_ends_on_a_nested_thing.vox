(Compile-fail: plan 310 - a chain ending on a nested thing names the whole
 thing. Copying one is ordinary (§5), but printing one recurses through its
 fields, which is the printing task (§7) - so this errors for now rather
 than printing the first eight bytes of the nested shape.)

A thing called point has
  a number called x is 0.

A thing called segment has
  a point called start.

a segment called edge.
Print edge's start.
