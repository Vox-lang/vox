(Compile-fail: plan 310 - a chain must end on a field that holds a value.
 Ending on a nested thing means reading the whole thing, which is copying
 (§5) or printing (§7) - both later tasks, so this errors for now rather
 than reading the first eight bytes of the nested shape.)

A thing called point has
  a number called x is 0.

A thing called segment has
  a point called start.

a segment called edge.
Print edge's start.
