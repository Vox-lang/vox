cannot render this value, which is a float, in hexadecimal
note: `:x`, `:X`, `:b` and `:o` write a whole number in another base, and a float is not one
help: work it out into a number first - `a number called total is {...} as a number.` - and render that, or drop the specifier to render the value itself
