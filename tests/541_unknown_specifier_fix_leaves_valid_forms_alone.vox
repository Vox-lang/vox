(Bug #98: the fix that refuses an unrecognised format specifier must not
 touch the specifiers the compiler already renders correctly - the same
 hex and width forms from the bug's own repro, minus the three bad ones.)

a number called n is 255.
Print "{n:x}|{n:8}|".
